{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "lerna": "^3.16.4",
    "lodash.camelcase": "^4.3.0"
  },
  "scripts": {
    "watch": "lerna run watch --parallel --stream",
    "build": "lerna run build --stream",
    "pretest": "lerna run build",
    "test": "lerna run test --stream",
    "lint": "lerna run lint --stream",
    "lint:fix": "lerna run lint:fix --stream",
    "release": "lerna bootstrap && npm run test && npm run build && lerna publish --force-publish=* --conventional-commits",
    "prerelease": "lerna bootstrap && npm run test && npm run build && lerna publish --dist-tag prerelease --force-publish=* --preid rc"
  },
  "dependencies": {}
}
